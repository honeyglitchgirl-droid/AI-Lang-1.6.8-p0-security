# CUDA / Accelerator notes

Architecture supports an Accelerator device path.

**Current status:** host-side stub only. Real CUDA (cudarc) is planned, not implemented.

Env: `AI_LANG_ACCELERATOR=1` makes the device appear available for policy testing; numerics still run on host.

See ROADMAP and RELEASE_STATUS.
